[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc = "0.8.24"
optimizer = true
optimizer_runs = 200
via_ir = true # executeGuarded's balance-delta + oracle-floor locals need the IR pipeline

[rpc_endpoints]
mantle_sepolia = "${MANTLE_RPC_URL}"
